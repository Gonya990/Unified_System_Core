---
description: Full system synchronization (Copy of /sync)
---

# 💎 Sinc (Full Orchestration)

This is an alias for the main synchronization workflow.

// turbo-all

1. Run the Vibranium Sync orchestrator:
   `bash Scripts/Orchestration/vibranium-sync.sh`

2. Verify that the final state is clean:
   `git status`
