apiVersion: v1
kind: ConfigMap
metadata:
  name: ai-telegram-bot-config
  namespace: trading
data:
  # GKE credentials are injected via Workload Identity, not stored here.
  # See: https://cloud.google.com/kubernetes-engine/docs/how-to/workload-identity
  PYTHONPATH: "/app:/app/src"
  LOG_LEVEL: "INFO"
