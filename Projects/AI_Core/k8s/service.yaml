apiVersion: v1
kind: Service
metadata:
  name: ai-telegram-bot
  namespace: trading
  labels:
    app: ai-telegram-bot
spec:
  type: ClusterIP
  selector:
    app: ai-telegram-bot
  ports:
    - port: 8080
      targetPort: health
      protocol: TCP
      name: health
