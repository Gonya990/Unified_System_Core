# Browser Agent Activation Plan

## Goal
Connect the existing [browser_agent.py](file:///home/gonya/01_Projects/PRJ-004_AI_Agents/02_Dev/browser_agent.py) script to the newly deployed **headless Chrome** container to enable AI-driven browser automation on this server.

## User Review Required
> [!IMPORTANT]
> **Dependencies**: I will need to install `playwright` and `httpx` in your Python environment.
> **Configuration**: I will modify [browser_agent.py](file:///home/gonya/01_Projects/PRJ-004_AI_Agents/02_Dev/browser_agent.py) to connect to `ws://127.0.0.1:9222` instead of launching a new browser process. This utilizes the Chrome instance we just set up.

## Proposed Changes
### Agent Configuration
#### [MODIFY] [browser_agent.py](file:///home/gonya/01_Projects/PRJ-004_AI_Agents/02_Dev/browser_agent.py)
- Change `async_playwright()` launch method to `connect_over_cdp()`.
- Target Endpoint: `http://localhost:9222`.

### Dependencies
- Run `pip install playwright httpx`.

## Verification Plan
1. **Dependency Check**: `python3 -c "import playwright"`
2. **Connectivity Check**: Run the agent. It should print `🌐 Browser Agent Started!` and successfully navigate to a test URL (e.g. `example.com`) using the Docker container.
